NODE_ENV=test
DB_PATH=./data/test.db
PORT=3000
SESSION_SECRET=test-secret-key 

# Umgebung
NODE_ENV=test
PORT=3000

# Datenbank (SQLite für Tests)
DB_TYPE=sqlite
DB_PATH=./data/test.db

# PostgreSQL (für spätere Migration)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ski_manager_test
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# Session & Security
SESSION_SECRET=test-secret-key
COOKIE_SECRET=test-cookie-secret
JWT_SECRET=test-jwt-secret

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_WINDOW=900000  # 15 Minuten
RATE_LIMIT_MAX=100

# Cache
CACHE_TTL=600  # 10 Minuten
CACHE_ENABLED=false  # Deaktiviert für Tests

# Logging
LOG_LEVEL=debug
LOG_TO_FILE=false

# Test-Spezifisch
SKIP_AUTH=true
MOCK_EXTERNAL_SERVICES=true